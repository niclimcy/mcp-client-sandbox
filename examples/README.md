# Example MCP Servers

This directory is to keep recreations of malicious and vulnerable MCP servers. Please mark malicious servers with `malicious_` and vulnerable servers as `vulnerable_` when creating a project directory.
